// Operador de Coalescência nula

let saudacao = 'Seja bem vindo ao nosso curso de JavaScript'
let mensagem = saudacao ?? 'Seja bem vindo'

// Operador Ternário

const idade = 16
let maiorDeIdade = idade >= 18 ? 'maior de idade' : 'menor de idade'
console.log(maiorDeIdade)
